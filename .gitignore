# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environment
venv/
.venv/
env/

# Local database and logs
*.db
*.db-shm
*.db-wal
*.log

# AI models & test videos (large binary files)
*.pt
*.mp4
*.mov
*.MOV
models/

# IDE / OS
.idea/
.claude/
.DS_Store
*.swp

# License — never commit signing keys or one-off test licenses
dev_license_private_key.pem
dev_license.jwt
# Keep license_public_key.pem tracked — detectors need it to verify backend tokens.

# Machine-specific exported Windows CA bundle (regenerated per host, contains
# that host's TLS-interception root). Not portable — never commit.
win-ca-bundle.pem
service/logs/
